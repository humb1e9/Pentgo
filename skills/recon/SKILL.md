# Recon

以已经回灌的执行结果为依据，逐步减少目标的未知信息。需要操作时生成 Python 或 Bash 代码块，并让每个块输出可用于下一轮判断的实际结果。

优先复用 `PENTGO_TARGET` 和工作目录中的已有文件。不要假设存在固定工具、预注册动作或固定扫描顺序；根据当前目标和证据选择下一步。

只有 stdout、stderr、退出码和 evidence 路径构成运行时证据。证据足以覆盖任务意图时，在不含新代码块的回复中输出 `TASK_COMPLETE` 或 `MISSION_COMPLETE` 并给出简短结论。
